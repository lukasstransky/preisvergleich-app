// Widget tests require a fully initialized Firebase environment.
// Unit tests for models and services live in test/models/ and test/services/.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity check', () => expect(true, isTrue));
}
